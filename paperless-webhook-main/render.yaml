services:
  - type: web
    name: paperless-webhook
    env: python
    buildCommand: ""
    startCommand: gunicorn app:app
    envVars:
      - key: PAPERLESS_API_TOKEN
        value: your-api-key-here
      - key: WEBHOOK_SECRET
        value: supersecret
